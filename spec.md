# 台鐵通勤最佳班次通知系統 — 規格書

> **專案代號**：`tra-commute-bot`
> **版本**：v0.1（規格定案，待 API 探測驗證）
> **文件日期**：2026-08-18
> **目標讀者**：實作者（即本人）

---

## 1. 專案目標

每個工作日早上 **07:50**，自動向 Telegram 推送一則「今日通勤簡報」，內容為根據台鐵**即時誤點資料**計算出的**最佳搭乘班次**，讓使用者在起床當下就能決定幾點出門、搭哪一班車，避免到了車站才發現誤點而措手不及。

### 1.1 成功標準

| 指標 | 目標 |
|---|---|
| 通知準時率 | 07:50:00 ± 30 秒送達 |
| 推薦正確率 | 推薦班次實際可搭乘且為最早抵達台北者 ≥ 95% |
| 靜默失敗次數 | **0**（任何失敗都必須發出降級通知） |
| 端到端執行時間 | < 10 秒 |

### 1.2 非目標（Out of Scope，v0.1）

- 不處理台鐵以外的運具（高鐵、客運、計程車）
- 不做訂票、劃位
- 不提供 Web UI 或行動 App
- 不做多使用者、多路線支援（單人單路線硬編在設定檔）
- 不做 iOS 捷徑整合

---

## 2. 使用情境（User Story）

> 我是住在桃園、在松江南京上班的通勤族。
> 07:50 鬧鐘響，我伸手拿手機，Telegram 已經有一則訊息告訴我：
> 「今天 2008 誤點 12 分，改搭 1136（誤點 3 分），08:19 桃園發車，09:05 抵台北，預計 09:25 打卡，還有 5 分鐘餘裕。」
> 我於是知道要提早 5 分鐘出門，而不是照平常節奏出門後在月台上乾等。

### 2.1 使用者的通勤路徑

```
07:50 起床
  ↓ 30 分鐘（盥洗、早餐、步行/騎車到車站）
08:20 抵達台鐵桃園站（可搭乘起始時間 T_ready）
  ↓ 搭乘台鐵（桃園 1080 → 臺北 1000）
09:02 抵達台北車站（以 2008 準點為例）
  ↓ 約 20 分鐘（捷運 台北車站 →(淡水信義線)→ 中山 →(松山新店線)→ 松江南京 + 步行到公司）
09:22 打卡（截止 09:30）
```

### 2.2 常搭班次

| 車次 | 車種 | 桃園發車 | 臺北到達 | 行駛 | 悠遊卡 |
|---|---|---|---|---|---|
| 1136 | 區間車 | 08:16 | 08:57 | 41 分 | ✅ |
| **2008** | **區間快** | **08:26** | **09:02** | **36 分** | ✅ |
| 1138 | 區間車 | 08:34 | 09:14 | 40 分 | ✅ |
| 1142 | 區間車 | 08:46 | 09:34 | 48 分 | ✅ |
| 278 | 自強(3000) | 08:55 | 09:27 | 32 分 | ✅ 站票 |

> ✅ **已由 `probe-tdx.sh` 於 2026-08-18 實測校正**（TDX `DailyTrainTimetable/OD/1080/to/1000`，當日共 106 班次）。
> 程式仍**不得硬編時刻**，一律以 API 當日時刻表為準；設定檔只硬編車次編號。上表僅供人工驗證測試案例之用。



---

## 3. 需求釐清結果與假設

以下為需求訪談中確認或推論的項目，實作前若有出入須先修正本文件。

| # | 項目 | 決議 | 狀態 |
|---|---|---|---|
| A1 | 通知時間 | 07:50（原始需求寫 8:50，為筆誤） | ✅ 已確認 |
| A2 | 「9:24 到達桃園高鐵站」 | 應為「**08:24 到達台鐵桃園站**」 | ✅ 已確認 |
| A3 | 「1336」 | 應為 **1136** 之筆誤 | ✅ 已確認 |
| A4 | 抵達車站時間 T_ready | 預設 **08:20**（= 通知時間 + 30 分鐘準備），設定檔可調 | ✅ 已確認 |
| A5 | 台北車站 → 打卡所需時間 | 預設 **20 分鐘**，設定檔可調 | ✅ 已確認 |
| A6 | 打卡截止 | 09:30 | ✅ 已確認 |
| A7 | 候選班次範圍 | 不限於 2008/1136/1138，掃描時間窗內所有可搭班次；但這三班標記為「常搭」優先顯示 | 設計決策 |
| A8 | 執行日與通知時間 | 使用者需能**自行調整星期與時刻**，補班日以手動日期清單處理，不接政府行事曆（見 §10.3） | ✅ 已確認 |
| A9 | **無法準時抵達時的行為** | 不得僅告知失敗，須主動計算補償方案。補償手段**僅限「提早出門」**，不納入計程車等替代末端路徑 | ✅ 已確認 |
| A10 | **誤點證明** | 櫃檯可開立**當下所有誤點車次**的證明，不限實際搭乘班次（使用者實測）；唯僅能開立已到站列車。系統須指出該申請哪一班（見 §7.6） | ✅ 已確認（實測） |
| A11 | 排序主鍵 | **以「實際抵達時間最早」為準**。因 A10 證明可跨車次開立，遲到歸屬不影響決策 | ✅ 已確認 |
| A13 | **可搭乘車種限制** | 篩選依據是**是否接受電子票證**，與是否對號無關。普悠瑪、太魯閣不收電子票證須排除；自強、莒光可持卡上車（站票），照常納入候選且不加註記 | ✅ 已確認（使用者更正） |
| A12 | 多路線支援 | v0.1 僅單向（桃園→臺北）。回程／多路線列入 v0.2，但**設定檔結構須預留擴充空間**（見下） | ✅ 已確認 |

> **A4 與 A5 是整個系統唯二的「人肉參數」，也是最容易造成推薦錯誤的來源。**
> 建議上線後手動記錄一週的實際數據（幾點到車站、幾點打卡），再回頭校正。系統會在每日訊息尾端附上這兩個參數的當前值，方便隨時察覺偏差。
>
> 特別注意 A5：`last_mile_minutes = 20` 是**捷運路徑**（台北車站 →(淡水信義線)→ 中山 →(松山新店線)→ 松江南京 + 步行）的估計值。依 A9，系統不搜尋替代末端路徑，因此此值為單一常數，其準確度直接決定所有遲到計算的正確性。

---

## 4. 技術選型

### 4.1 語言：Go

| 考量 | Go | Python |
|---|---|---|
| 部署 | ✅ 單一靜態執行檔，`scp` + `systemd` 即可，零執行期依賴 | ❌ 需管理 venv / uv；系統 Python 升級可能弄壞依賴 |
| 啟動時間 | ✅ < 10 ms，適合 cron 短命任務 | ⚠️ ~100 ms（實務上無感） |
| JSON 結構處理 | ✅ TDX 巢狀結構（`StationName.Zh_tw`）用 struct 定義清楚，欄位缺漏編譯期就能發現 | ⚠️ dict 存取容易 typo，執行期才炸 |
| 時間運算 | ✅ `time.Time` 型別安全，時區明確 | ⚠️ naive/aware datetime 混用是經典坑 |
| 開發速度 | ⚠️ 較慢 | ✅ 快 |
| 長期維運 | ✅ 幾乎不用動 | ⚠️ 依賴需定期更新 |

**結論：採用 Go。** 本專案邏輯規模約 500–800 行，需求已明確、不需探索性開發，Python 的原型速度優勢用不上；而「丟上伺服器跑三年不用管」正是 Go 的主場。

**Go 版本**：1.22+
**外部依賴**：盡量只用標準函式庫。允許 `gopkg.in/yaml.v3`（設定檔解析）。HTTP、JSON、時間全部用 stdlib。

> 🔑 **關鍵細節**：容器或極簡系統可能沒有 tzdata，導致 `time.LoadLocation("Asia/Taipei")` 失敗。在 `main.go` 加入 `import _ "time/tzdata"` 將時區資料庫內嵌進執行檔，一勞永逸。

### 4.2 執行環境：Linode + systemd timer

**不使用 GitHub Actions 作為主排程。** GitHub Actions 的 `schedule` 觸發延遲 5–20 分鐘是常態（整點尤其嚴重，因為全球排隊）。本系統從通知（07:50）到發車（08:26）只有 36 分鐘緩衝，延遲 15 分鐘即失去意義。systemd timer 可精確到秒，且 Linode 主機已存在，無額外成本。

**GitHub Actions 僅用於 CI**：push 時跑 `go vet`、`go test`、`golangci-lint`，並在 tag 時交叉編譯產出 release binary。

### 4.3 通知通道：Telegram Bot

選用理由：
- **排版能力最佳** — 可用 `<pre>` 區塊排等寬字比較表，這正是本系統的核心輸出形式
- **訊息永久保留** — 可回頭查閱歷史，累積數月後能統計哪班車常誤點
- **零成本、API 穩定**
- **未來可擴充互動** — inline keyboard 做「重新查詢」按鈕不需改架構

已知限制：無法強制響鈴、可能被 iOS 專注模式攔截。**緩解措施**：在 iOS 設定中將 Telegram 加入「專注模式允許通知」清單，並為該對話開啟「時效性通知」。

---

## 5. 系統架構

```
┌─────────────────────────────────────────────────────┐
│  Linode (Ubuntu, TZ=Asia/Taipei)                    │
│                                                     │
│  systemd timer ──07:50:00 Mon-Fri──► systemd service│
│                                            │        │
│                                            ▼        │
│                                    ┌───────────────┐│
│                                    │ tracommute    ││
│                                    │  (Go binary)  ││
│                                    └───┬───────┬───┘│
└────────────────────────────────────────┼───────┼────┘
                                         │       │
                    ┌────────────────────┘       └──────────┐
                    ▼                                       ▼
         ┌──────────────────────┐              ┌────────────────────┐
         │  TDX API             │              │  Telegram Bot API  │
         │  ・OAuth2 Token      │              │  sendMessage       │
         │  ・DailyTrainTimetable│             └────────┬───────────┘
         │  ・TrainLiveBoard    │                       │
         │  ・StationLiveBoard  │                       ▼
         └──────────────────────┘                  📱 iPhone
```

### 5.1 執行流程

```
1. 載入設定 + 環境變數（憑證）
2. 判斷今日是否為執行日（週一~週五）→ 否則直接結束
3. 取得 TDX access token（OAuth2 client_credentials）
4. 循序抓取（間隔 request_interval_ms，遇 429 退避重試 — 見 §6.5）：
   ├─ 今日 OD 時刻表（桃園 → 臺北）
   └─ 列車即時位置動態（TrainLiveBoard）← 唯一誤點來源
5. 合併資料 → 建立候選班次清單
6. 執行排序演算法 → 產出推薦 + 備選
7. 渲染 Telegram 訊息
8. 發送（失敗則重試 3 次）
9. 結構化日誌寫入 stdout → journald
10. 原始 API 回應存檔（保留 30 天）
```

**設計原則：步驟 3–8 任一步失敗，都必須送出降級通知**（見 §9.3），絕不可靜默結束。

---

## 6. TDX API 介接

### 6.1 認證

```
POST https://tdx.transportdata.tw/auth/realms/TDXConnect/protocol/openid-connect/token
Content-Type: application/x-www-form-urlencoded

grant_type=client_credentials
&client_id={TDX_CLIENT_ID}
&client_secret={TDX_CLIENT_SECRET}
```

回傳 `access_token`（JWT，有效期約 24 小時）。後續請求帶 `Authorization: Bearer {token}`。

由於每天只執行一次，**不需快取 token**，每次重新取即可。若未來增加「出門前二次查詢」，再實作檔案快取（`/var/lib/tra-commute/token.json`，權限 0600）。

### 6.2 使用的端點

Base URL：`https://tdx.transportdata.tw/api/basic/v3/Rail/TRA`

| 用途 | 端點 | 說明 |
|---|---|---|
| 當日 OD 時刻表 | `GET /DailyTrainTimetable/OD/{origin}/to/{dest}/{date}` | 桃園→臺北今日所有直達班次；`{date}` 為 `yyyy-MM-dd`，**無 `/Today` 變體** |
| 列車即時位置動態 | `GET /TrainLiveBoard` | 全線列車的 `DelayTime`，**主要誤點來源** |
| ~~車站即時到離站~~ | ~~`GET /StationLiveBoard/Station/{stationID}`~~ | **v0.1 不使用**，見 §6.6 |
| 車站基本資料 | `GET /Station` | 僅初次建置時用來查證站碼，正式執行不呼叫 |
| 營運通阻事件 | `GET /News` | v0.2；**停駛偵測已可由時刻表的 `SuspendedFlag` 取得，不必為此呼叫** |

**參數**：所有請求加上 `?$format=JSON`。時刻表查詢可加 `$filter` 縮小範圍，但 v3 的 OData 支援度需實測。

### 6.3 站碼

| 車站 | StationID |
|---|---|
| 臺北 | `1000` |
| 桃園 | `1080` |

### 6.4 實測結果（2026-08-18，`probe-tdx.sh`）

| # | 項目 | 結果 |
|---|---|---|
| 1 | 站碼 | ✅ `1080` = 桃園、`1000` = 臺北 |
| 2 | 日期格式 | ✅ **`yyyy-MM-dd`**；`yyyy/MM/dd` 回 404 |
| 3 | `/DailyTrainTimetable/Today/OD/...` | ❌ **端點不存在（404）**，必須帶日期 |
| 4 | v3 回應形狀 | ✅ 皆為物件包一層陣列，非裸陣列 |
| 5 | 時間欄位格式 | ✅ **`"HH:mm"`**（非 `HH:mm:ss`），naive、無時區 |
| 6 | `DelayTime` 負值 | 單次取樣（140 班）未出現負值。**clamp 仍保留**為防禦性措施 |
| 7 | Token 有效期 | ✅ 86400 秒 |
| 8 | **API 速率限制** | ⚠️ **連續約 8 次請求後回 429**；退避重試有效，見 §6.5 |
| 9 | `StationLiveBoard` v3 | ❌ **僅回傳 1 筆，不堪使用** → v0.1 移除，見 §6.6 |
| 10 | `TrainLiveBoard/TrainNo/{no}` | ✅ 端點存在（但全量查詢更省請求數） |
| 11 | 車種代碼 | 區間 = ID `1131`/Code `6`；區間快 = `1132`/`10`；自強(3000) = `110H`/`11` |

#### 各端點回應鍵

```
/Station                → { UpdateTime, ..., Stations: [...] }        （245 站）
/DailyTrainTimetable/OD → { UpdateTime, TrainDate, TrainTimetables: [...] }
/TrainLiveBoard         → { UpdateTime, ..., TrainLiveBoards: [...] } （運行中 140 班）
```

#### `TrainTimetables[]` 結構

```
TrainInfo:  TrainNo, RouteID, Direction, TrainTypeID, TrainTypeCode,
            TrainTypeName{Zh_tw,En}, TripHeadSign,
            StartingStationID/Name, EndingStationID/Name, OverNightStationID,
            TripLine, WheelChairFlag, PackageServiceFlag, DiningFlag,
            BreastFeedFlag, BikeFlag, CarFlag, DailyFlag, ExtraTrainFlag,
            SuspendedFlag, ServiceType, Note
StopTimes[]: StopSequence, StationID, StationName{Zh_tw,En},
             ArrivalTime, DepartureTime, SuspendedFlag
```

> 💡 **意外收穫**：`SuspendedFlag` 同時存在於 `TrainInfo`（整車停駛）與 `StopTimes`（該站不停）。這表示**停駛偵測不需要額外呼叫 `/News`**，當日時刻表本身就帶這個資訊。原訂 v0.2 的功能可以在 v0.1 順手做掉，只是多一個 `if`。

#### `TrainLiveBoards[]` 結構

```
TrainNo, TrainTypeID, TrainTypeCode, TrainTypeName{Zh_tw,En},
StationID, StationName{Zh_tw,En}, TrainStationStatus, DelayTime, UpdateTime
```

`TrainStationStatus` 語意未知（取樣值為 `2`），推測為進站／在站／離站。實作時**不依賴此欄位**，僅記錄備查。

`UpdateTime` 為 **RFC3339 含時區**（`2026-08-18T14:26:40+08:00`），與時刻表的 naive `HH:mm` 不同，解析時勿混用。

### 6.5 ⚠️ 速率限制與請求節流

探測腳本連續發出約 7 個請求後即開始收到：

```json
{ "message": "API rate limit exceeded" }   // HTTP 429
```

這**直接影響正式程式的設計**。原規劃「並行抓取三支端點」會在單一時間點打出 3 個併發請求，有觸發 429 的風險。

**修訂後的請求策略**：

1. **改為循序請求，而非並行**。本流程對延遲不敏感（每天執行一次，總耗時預算 10 秒），沒有理由並行。
2. 每次請求之間插入間隔（`request_interval_ms`，預設 1500）。
3. 遇 429 時以指數退避重試（5s → 10s → 15s），最多 3 次。
4. 重試耗盡則走 §9.3 降級通知——**寧可送出「僅表定時刻」的訊息，也不可靜默失敗**。

正式流程僅需 **3 個請求**（token + 時刻表 + `TrainLiveBoard`，見 §6.6），在此策略下總耗時約 5 秒，遠低於預算。

> 實測佐證：探測腳本在約第 8 個請求後觸發 429，經 5s→10s→15s 三次退避後成功。退避策略有效，但正式流程根本不該逼近上限。

> 具體限制數值 TDX 未公開，且可能依會員等級而異。**不要試圖精確逼近上限**——這是每天只跑一次的程式，多等幾秒毫無成本，被鎖住卻會讓你錯過火車。

### 6.6 `StationLiveBoard` v3 實測結論：**不採用**

第二輪探測（2026-08-18 14:37）驗證了先前的疑慮，結果比傳聞更糟：

| 來源 | 桃園站看板筆數 | 未來班次 |
|---|---|---|
| v3 `StationLiveBoard/Station/1080` | **1 筆** | 1 筆 |
| v2 `LiveBoard/Station/1080` | 4 筆 | 4 筆 |

v3 並非「只回傳過去班次」，而是**幾乎不回傳任何東西**——單一車站只給 1 筆資料，資訊量遠低於 v2，也遠低於一個站別看板該有的樣子。

而且它的欄位與時刻表 API **不一致**，這是實作時極易踩到的坑：

| API | 欄位名 | 時間格式 |
|---|---|---|
| `DailyTrainTimetable` → `StopTimes` | `ArrivalTime` / `DepartureTime` | **`"12:43"`**（HH:mm） |
| `StationLiveBoard` v3 | `ScheduleArrivalTime` / `ScheduleDepartureTime` | **`"14:42:00"`**（HH:mm:ss） |

注意是 `Schedule` 而非 `Scheduled`（無 `d`），與 v2 的命名也不同。**Go 端若共用一套時間解析函式必然出錯**，須為兩種格式各寫一個 parser，或統一以 `HH:mm` 截斷處理。

> 探測腳本第一版正是栽在這裡：用了 `ScheduledDepartureTime`，欄位取不到、統計全歸零，差點誤判成「看板真的沒有未來班次」。已修正。

#### 決策：v0.1 完全移除 `StationLiveBoard`

`TrainLiveBoard` 一次請求即可取得**全線所有運行中列車**（實測 141 筆）的 `DelayTime`，已完整涵蓋需求。保留 `StationLiveBoard` 只會帶來：

- 多一次請求 → 更逼近速率上限（實測此端點需退避重試 3 次、耗時 30 秒才成功）
- 多一組欄位命名與時間格式 → 更多解析錯誤面
- 交叉驗證樣本僅 1 筆 → 統計上毫無意義

**移除它同時簡化了三件事，卻沒有損失任何資訊。** 若日後真需要月台號碼（`Platform` 欄位僅此端點有），再評估以 v2 補上，並註記 v2 有停用風險。

正式流程因此縮減為 **3 次請求**：token → 當日 OD 時刻表 → `TrainLiveBoard`。

### 6.7 尚待驗證

- [ ] `/TrainType` 完整對照表，找出普悠瑪／太魯閣的 `TrainTypeID`（A13）
- [ ] `TrainStationStatus` 欄位語意（實測值恆為 `2`，不依賴）

---

## 7. 演算法：最佳班次選擇

這是系統的核心。目標：**在「我趕得上」的前提下，找出「最早抵達台北」的班次。**

### 7.1 名詞定義

| 符號 | 意義 | 來源 |
|---|---|---|
| `T_ready` | 使用者最早可抵達桃園站的時刻 | 設定：通知時間 + `ready_lead_minutes` |
| `T_deadline` | 打卡截止時刻（09:30） | 設定 |
| `last_mile` | 台北車站 → 打卡所需分鐘數（20） | 設定 |
| `board_buffer` | 進站到上車的緩衝分鐘數（2） | 設定 |
| `risk_margin` | 判定「風險班次」的門檻分鐘數（3） | 設定 |
| `sched_dep_i` | 班次 i 在桃園的表定發車時刻 | TDX 時刻表 |
| `sched_arr_i` | 班次 i 在臺北的表定到達時刻 | TDX 時刻表 |
| `delay_i` | 班次 i 目前的誤點分鐘數 | TDX 即時動態 |

### 7.2 步驟一：建立候選清單

從當日 OD 時刻表取出所有**停靠桃園且停靠臺北**的班次，篩選時間窗：

```
sched_dep_i ∈ [T_ready − 30min, T_ready + 60min]
```

> 為什麼要往前抓 30 分鐘？因為**表定已經開走的車，可能因為誤點而還沒開走**。這正是使用者需求中 1136 的情境：表定 08:16 早於 T_ready 08:20，看似錯過，但誤點 10 分鐘後實際 08:26 才發車，仍然搭得上。**不往前抓就會漏掉這類機會。**

#### 車種篩選（A13）

候選清單須排除**不接受電子票證的車種**。注意這與「是否對號」無關：

| 車種 | 對號 | 悠遊卡 | 處置 |
|---|---|---|---|
| 區間車、區間快 | 否 | ✅ | 納入 |
| 自強、莒光 | 是 | ✅ 可買站票 | **照常納入，不加註記** |
| 普悠瑪、太魯閣 | 全車對號 | ❌ | **排除** |

> ⚠️ **不可用 `TrainTypeCode` 篩選。** 普悠瑪與太魯閣在票種分級上屬自強等級，`TrainTypeCode` 很可能與一般自強共用同一個值（實測 自強(3000) = `"11"`）。必須改用 `TrainTypeID` 或 `TrainTypeName.Zh_tw` 判別。

篩選採**排除清單**而非允許清單，理由是台鐵未來新增車種時，允許清單會靜默漏掉可搭的班次，而排除清單只會多納入一班你自己看得出來的車。

遇到清單外的未知車種時，依 `unknown_train_type_policy`：
- `include_and_flag`（預設）：納入候選，但在訊息中標示「⚠️ 車種未知，請確認可否持電子票證乘車」，同時寫入日誌供事後補進清單
- `exclude`：保守排除

> 選 `include_and_flag` 為預設，是因為兩種錯誤的代價不對稱：誤推薦一班搭不上的車，你在月台上會發現並改搭下一班；誤排除一班可搭的車，你永遠不會知道自己錯過了更好的選擇。前者可觀察、可修正，後者不可見。

### 7.3 步驟二：套用即時誤點

對每個候選班次 i：

```go
delay_i = lookupDelay(trainNo_i)   // 優先 TrainLiveBoard，次之 StationLiveBoard，皆無則 0
if delay_i < 0 { delay_i = 0 }     // 台鐵偶有負值；列車不會早發，clamp 到 0

est_dep_i = sched_dep_i + delay_i
est_arr_i = sched_arr_i + delay_i
```

**誤點傳播模型（保守假設）**：假設列車在剩餘路段**不會補回誤點**，即抵達臺北的誤點等於當前誤點。

理由：桃園→臺北僅約 35 分鐘、途中站點少，補時空間本就有限；且低估誤點會導致「以為來得及卻遲到」，高估只會讓人早到。**在此情境下，保守方向的錯誤成本遠低於樂觀方向。** 因此不引入衰減係數。

若 `delay_i` 無即時資料，標記 `DelaySource = None` 並在輸出中明確標示「表定」，讓使用者知道這筆數字不可盡信。

### 7.4 步驟三：分類（二維模型）

> **設計修訂（v0.1 → v0.2 草案）**：早期版本把班次歸入單一列舉 `SAFE / RISKY / LATE / MISSED`，但這**混淆了兩個彼此獨立的軸**——「我搭不搭得上」和「搭上後會不會遲到」是兩回事。一班車完全可能既難搭上、又會遲到；把它們壓成一維會讓「有一班很好搭但會遲到 3 分鐘」和「有一班很難搭但能準時」無法正確比較。因此改為二維模型。

**軸一：可搭乘性（Catchability）** — 取決於 `est_dep`

```
MISSED      est_dep_i < T_ready + board_buffer
            → 趕不上

RISKY       T_ready + board_buffer ≤ est_dep_i < T_ready + board_buffer + risk_margin
            → 理論上趕得上，但餘裕不足

CATCHABLE   est_dep_i ≥ T_ready + board_buffer + risk_margin
            → 可安全搭乘
```

**軸二：遲到量（Lateness）** — 取決於 `est_arr`，是**連續量而非布林值**，且須拆分歸屬

```
lateness_i     = max(0, est_arr_i   + last_mile − T_deadline)   總遲到
```

把遲到量化成分鐘數（而非只是 LATE 標記）是滿足 A9 的關鍵：**遲到 2 分鐘和遲到 25 分鐘是完全不同的處境**，前者可能低調進辦公室就算了，後者得考慮提早出門或申請誤點證明。

> **為什麼需要 RISKY 這一層？**
> 台鐵的 `DelayTime` 是浮動的——列車可能在你出門途中「趕回來」，原本誤點 10 分變成誤點 4 分，於是提早開走，你就撲空了。若某班次只靠誤點才勉強趕得上（`est_dep` 與 `T_ready` 只差 1–2 分鐘），它本質上是**建立在一個隨時會消失的前提上**，不應該當成主要推薦。
>
> 使用者原始需求中的例子（T_ready=08:20、1136 誤點後 08:26 發車）差距 6 分鐘，在預設參數（`board_buffer=2`、`risk_margin=3`）下屬於 CATCHABLE。但若當天誤點只有 3 分鐘（08:19 發車），就會落入 MISSED——這正是需要即時資料的原因。

### 7.5 步驟四：排序與推薦

採用**字典序（lexicographic）排序，而非加權評分**。

> **設計決策**：加權評分（如 `score = 0.7×抵達時間 + 0.3×風險`）看似彈性，實際上會讓推薦結果難以預測、難以除錯——某天推薦怪異時，你無法回答「為什麼」。使用者的偏好其實是嚴格分層的：**先確保搭得上，再要求最早到**。字典序精確反映這個心智模型，且行為完全可解釋。

排序鍵，由高到低：

```
1. Catchability：CATCHABLE > RISKY > MISSED
2. est_arr_i 升冪              ← 需求核心：以「實際抵達時間」判斷
3. est_dep_i 降冪              ← 同時抵達時，發車越晚越好（少在月台等）
4. DelaySource 可信度：有即時資料 > 無即時資料
5. 車次編號（穩定排序，確保結果可重現）
```

> **為什麼 `lateness`（總遲到）不在排序鍵中？**
> 在同一 Catchability 層內，`lateness = max(0, est_arr + last_mile − deadline)` 是 `est_arr` 的單調遞增函數——**按 `est_arr` 升冪排序，本身就等同於「總遲到分鐘數最小化」**。多列一個排序鍵不會改變結果，只會增加理解成本。

> **為什麼遲到歸屬不在排序鍵中？（A11）**
> 早期草案曾把遲到拆成「可免責／不可免責」並以後者為主鍵，理由是「誤點證明只能開立實際搭乘車次」，因此選了慢車就得自負遲到。**該前提經使用者實測推翻**（見 §7.6）：證明可跨車次開立，遲到的可免責性與你選哪班車無關。歸屬概念因此整個移除。

> **為什麼 CATCHABLE 層級高於一切？** 一班能準時抵達但很可能撲空的 RISKY 車，價值低於一班穩穩搭得上、只遲到 2 分鐘的 CATCHABLE 車——因為撲空的下場是搭到更後面的班次，遲到更久。不過當差距懸殊時（RISKY 準時 vs CATCHABLE 遲到 20 分），使用者應該自己決定要不要賭，所以訊息會把這個取捨明白呈現，而不是替他決定。

**推薦 = 排序後第一名。**
**備選 = 排序後第二、三名**，以及第一名以外的最佳 RISKY 班次（若有）。

---

### 7.6 誤點證明（需求 A10）

#### 實務前提（使用者實測，非推論）

先前草案假設「誤點證明只能開立你實際搭乘的那班車」，據此推導出一套警示邏輯。**此假設經使用者實地驗證後推翻**，正確的運作方式是：

| 項目 | 實際情形 |
|---|---|
| 開立範圍 | **不限實際搭乘車次**，向櫃檯詢問即可開出當下所有誤點車次的證明 |
| 票證形式 | 使用者以**電子票證（悠遊卡）**乘車，台鐵無從得知實際搭乘車次 |
| 唯一限制 | **僅能開立已經到站的列車** |
| 申請方式 | 迄站（臺北）櫃檯人工申請 |

這推翻了「選了慢車就開不出證明」的顧慮，也確認了 A11（以實際抵達時間為排序主鍵）是正確的——遲到的可免責性與你選哪班車無關。

#### 系統該做的事

既然證明可涵蓋任意誤點車次，系統就能**直接指出該申請哪一班**。

```
到站時刻      T_at_counter = est_arr_recommended        （你抵達臺北的時間）

可申請集合    C = { j ∈ 候選班次 : est_arr_j ≤ T_at_counter
                                  AND delay_j ≥ min_delay_minutes }

最佳證明      best = argmax_{j ∈ C} delay_j
可證明分鐘數  certifiable = delay_best   （C 為空時為 0）
涵蓋判定      covered = certifiable ≥ lateness_recommended
```

「僅能開立已到站列車」的限制就編碼在 `est_arr_j ≤ T_at_counter` 這個條件裡。實務上它很少真正生效（誤點最嚴重的車通常也是你前面那班），但成本只有一個比較運算，沒有理由不寫。

#### 輸出

當 `lateness_recommended > 0`：

| 情況 | 訊息 |
|---|---|
| `covered = true` | `📄 抵達後可申請 {best} 次誤點證明（誤點 {certifiable} 分），足以涵蓋今日遲到` |
| `certifiable > 0` 但未涵蓋 | `📄 可申請 {best} 次誤點證明（誤點 {certifiable} 分），但遲到 {lateness} 分，未必全數涵蓋` |
| `C` 為空 | `⚠️ 今日全線無顯著誤點，遲到可能無法取得證明` |

最後一種情況是唯一真正需要示警的：全線都準點、你卻仍會遲到，代表問題出在 `ready_lead_minutes` 設定或今天起晚了，而不是台鐵。這種時候沒有證明可開。

> **候選集合的近似**：嚴格來說「已到站的誤點列車」應涵蓋所有抵達臺北的車次，而非僅限桃園→臺北時間窗內的候選。本規格採用候選集合作為近似，因為它們正是與你同時段、同路線的列車，誤點狀況最具代表性，且無需額外 API 呼叫。若日後發現漏報，可改查 `StationLiveBoard/Station/1000`。

#### 界線

系統**不代為申請、不產生任何文件、不代擬給主管的訊息**。它只告訴你去櫃檯要報哪個車次號碼。

#### 已移除的概念

`own_lateness`（不可免責遲到）自本版起從模型中**完全移除**。它存在的唯一理由是那個已被推翻的假設；既然證明可跨車次開立，遲到就不再需要區分歸屬。移除後排序、輸出、測試各少一個維度。

---

### 7.7 情境驗證

以下情境必須全部通過測試（見 §11）。設定：`T_ready=08:20`、`board_buffer=2`、`risk_margin=3`，故 CATCHABLE 門檻為 `est_dep ≥ 08:25`。

**情境 A：全部準點**

| 車次 | 表定發車 | 表定到北 | 誤點 | 預計發車 | 預計到北 | 打卡 | 分類 |
|---|---|---|---|---|---|---|---|
| 1136 | 08:16 | 08:57 | 0 | 08:16 | 08:57 | — | MISSED |
| **2008** | 08:26 | 09:02 | 0 | 08:26 | 09:02 | 09:22 | **CATCHABLE** |
| 1138 | 08:34 | 09:14 | 0 | 08:34 | 09:14 | 09:34 | CATCHABLE（遲 4 分） |

→ 推薦 **2008**。符合日常預期。✅

**情境 B：全部誤點 10 分**

| 車次 | 表定發車 | 表定到北 | 誤點 | 預計發車 | 預計到北 | 打卡 | 分類 |
|---|---|---|---|---|---|---|---|
| **1136** | 08:16 | 08:57 | +10 | 08:26 | 09:07 | 09:27 | **CATCHABLE，準時** |
| 2008 | 08:26 | 09:02 | +10 | 08:36 | 09:12 | 09:32 | CATCHABLE（遲 2 分） |
| 1138 | 08:34 | 09:14 | +10 | 08:44 | 09:24 | 09:44 | CATCHABLE（遲 14 分） |

→ 推薦 **1136**：實際抵北 09:07 早於 2008 的 09:12，且剛好能準時打卡。

> ⚠️ **此結論推翻了原始需求的敘述。**
> 需求訪談時提到「全部誤點 10 分鐘時，2008 仍會比 1136 快抵達台北」。實測時刻表顯示 **1136 表定 08:57 抵北，比 2008 的 09:02 早 5 分鐘**——1136 雖是區間車、行駛較久（41 vs 36 分），但因為發車早 10 分鐘，仍先抵達。誤點相同時這個差距不會改變。
>
> 更關鍵的是**這 5 分鐘決定了會不會遲到**：1136 讓你 09:27 打卡（準時），2008 則是 09:32（遲到 2 分）。
>
> 這正是為什麼演算法必須用 `est_arr` 實算，而不是預設「區間快一定比較快」。人的直覺在這裡是錯的。

**情境 C：個別誤點不同（演算法的價值所在）**

| 車次 | 表定發車 | 表定到北 | 誤點 | 預計發車 | 預計到北 | 分類 |
|---|---|---|---|---|---|---|
| 1136 | 08:16 | 08:57 | +10 | 08:26 | 09:07 | CATCHABLE，準時 |
| 2008 | 08:26 | 09:02 | +25 | 08:51 | 09:27 | CATCHABLE，遲 17 分 |

→ 2008 抵北 09:27 + 20 分 = 09:47，遲到 17 分；1136 抵北 09:07 → 09:27 打卡，準時。
→ **推薦 1136**，並警示「今日首選 2008 嚴重誤點」。✅

**情境 D：邊界 — 只靠誤點才趕得上**

| 車次 | 表定發車 | 誤點 | 預計發車 | 與 T_ready 差距 | 分類 |
|---|---|---|---|---|---|
| 1136 | 08:16 | +6 | 08:22 | 2 分 | RISKY |
| 2008 | 08:26 | +6 | 08:32 | 12 分 | CATCHABLE |

→ 推薦 **2008**（CATCHABLE），但訊息中附註「1136 若你 08:22 前到站也搭得上，但誤點可能縮短，不建議賭」。✅

**情境 E：全線大誤點，怎樣都遲到**

所有候選的 `lateness > 0` → 推薦排序第一者（= 遲到最少者），訊息切換為警示模式並附上補償方案（見 §7.7）。✅

---

### 7.8 遲到補償方案（需求 A9）

當排序第一名的 `lateness > 0`，系統不得只是通知失敗，必須主動搜尋能縮短遲到的手段。

依 A9，**補償手段僅限「提早出門」**（調降 `T_ready`）。不搜尋計程車等替代末端路徑——使用者明確表示不需要，且引入成本維度會讓系統被迫做金錢與時間的價值判斷。

#### 演算法

某些班次目前被歸為 MISSED 或 RISKY，純粹是因為 `T_ready = 08:20` 這個假設。若願意提早 X 分鐘出門，這些班次就變得可搭。

```
對每個 catchability ∈ {MISSED, RISKY} 的班次 i：
    required_ready_i = est_dep_i − board_buffer − risk_margin
    early_leave_i    = T_ready − required_ready_i          （需提早幾分鐘出門）

    若 0 < early_leave_i ≤ max_early_leave_minutes
       且 est_arr_i < est_arr_best
    → 列為補償方案
```

搜尋空間極小（時間窗內至多約 10 班次），直接線性掃描即可。

輸出範例：**「提早 6 分鐘出門（08:14 到站）可搭 1136，抵北從 09:26 提前到 09:14，遲到從 16 分降為 4 分」**

> 這通常是唯一真正有價值的補償——代價只是早起幾分鐘，效果卻可能是「從遲到變成準時」。也是一維模型會漏掉的：MISSED 的班次在舊模型裡直接被丟棄，但它其實可能只差 4 分鐘。

#### 輸出優先序

```
1. 若存在 lateness = 0 的補償方案 → 「✅ 這樣做可以準時」
2. 否則取 est_arr 最早者          → 「⏰ 最少可壓到遲到 N 分鐘」
3. 一律同時顯示「什麼都不做」的基準線，讓效益差額一目瞭然
```

> **設計原則：系統提供選項與量化後果，不替使用者做價值判斷。**
> 「早起 6 分鐘換取早到 12 分鐘」划不划算，取決於當天有沒有晨會、昨晚幾點睡、這個月遲到幾次了——這些系統都不知道。它該做的是把選項講清楚，然後閉嘴。

#### 邊界：補償也救不了的情況

若所有方案的 `lateness` 仍 > `severe_delay_threshold`（預設 30 分），訊息升級為最高警示，並提示「今日建議直接聯繫主管」。系統**不代擬訊息、不自動通知任何人**——這超出授權範圍，且誤發的代價很高。

---

## 8. 設定檔

`/etc/tra-commute/config.yaml`（權限 0644，**不含任何憑證**）

```yaml
# ── 排程：改完存檔即生效，不需 sudo，不需 daemon-reload ──
schedules:
  - name: "平日通勤"
    weekdays: [Mon, Tue, Wed, Thu, Fri]
    at: "07:50"
  - name: "補班日"                    # 手動列出，不接政府行事曆
    dates: ["2026-09-26", "2026-12-19"]
    at: "07:50"

skip_dates: []                        # 請假、國定假日、出差
tick_tolerance_minutes: 2             # 容忍排程觸發稍晚仍視為命中

timing:
  ready_lead_minutes: 30              # 通知後多久能抵達桃園站 → T_ready = at + 30

# v0.1 為單一路線。v0.2 將改為 journeys 清單，屆時 schedules 各項可帶
# 選用的 journey 鍵；未指定者沿用預設路線，因此本結構不會成為破壞性變更。
route:
  origin_station_id: "1080"           # 桃園
  origin_name: "桃園"
  destination_station_id: "1000"      # 臺北
  destination_name: "臺北"
  usual_train_nos: ["2008", "1136", "1138"]  # 標記為「常搭」，不限制候選範圍
  lookback_minutes: 30                # 往前掃描（撈取因誤點而尚未發車的班次）
  lookahead_minutes: 60
  # A13：排除不接受電子票證的車種（與是否對號無關）
  # 自強、莒光可持悠遊卡搭乘站票，照常納入候選
  excluded_train_type_ids: []              # ⏳ 以 /TrainType 查證後填入
  excluded_train_type_keywords: ["普悠瑪", "太魯閣"]   # 名稱比對，防禦性後備
  unknown_train_type_policy: "include_and_flag"       # include_and_flag | exclude

constraints:
  clock_in_deadline: "09:30"
  last_mile_minutes: 20               # 台北車站 →中山→ 松江南京 + 步行
  boarding_buffer_minutes: 2
  risk_margin_minutes: 3

certificate:                          # §7.6 誤點證明
  enabled: true
  min_delay_minutes: 5                # 低於此誤點分鐘數的車次不值得專程去開
  note: "臺北站櫃檯人工申請，可開立當下所有已到站誤點車次"

compensation:                         # §7.8 遲到補償
  enabled: true
  max_early_leave_minutes: 15         # 最多可接受提早多久出門（已確認）
  severe_delay_threshold: 30          # 遲到超過此值升級為最高警示

output:
  max_alternatives: 4
  timezone: "Asia/Taipei"
```

憑證一律走環境變數，存於 `/etc/tra-commute/env`（權限 **0600**，owner `tracommute`）：

```bash
TDX_CLIENT_ID=xxxxx
TDX_CLIENT_SECRET=xxxxx
TELEGRAM_BOT_TOKEN=xxxxx
TELEGRAM_CHAT_ID=xxxxx
```

> 憑證**絕不進 git**。`.gitignore` 加入 `env`、`*.local.yaml`。

---

## 9. 輸出格式

### 9.0 版型限制（實機測試結論，2026-08-18）

以下兩點由實機（iPhone / Telegram iOS）驗證得出，**優先於 §9.1 / §9.2 的其餘版型描述**。

#### 一、`<pre>` 內只能使用 ASCII

Telegram 的等寬字體中，**中文字寬並非拉丁字元的剛好兩倍**。因此任何「以 East Asian Width 計算 cell 數、湊成矩形」的做法，在手機上仍然會歪掉——不論寬度算得多精確。

結論：`<pre>` 區塊內一律只放 ASCII，欄位標題改用英文縮寫（`NO.` / `DLY` / `DEP` / `ARR` / `LATE`），並在表格**下方**以中文對照說明。中文只出現在不需對齊的散文區域。

#### 二、不使用 emoji，改用粗體與純文字符號

原版型以 emoji 作狀態標記（`✅建議`、`⏰遲到` 等）。實測有兩個問題：

1. 每個 emoji 佔 2 個 cell，配上框線表格後總寬達 **40 字元**，正好落在 Telegram 手機端的 wrap 門檻上；一旦 wrap 就毀掉整個對齊，而對齊正是使用 `<pre>` 的唯一理由
2. emoji 寬度同樣不可靠（variation selector 的行為隨客戶端版本而異）

改用 `<b>` 粗體建立標題階層、單一 ASCII 字元作列標記（表格下方附圖例），並移除 `┌─┬┐` 框線與儲存格內邊距。**總寬從 40 降到 24 字元**，窄螢幕直式也不會 wrap。

#### 三、`<pre>` 內行首不得為空白

**Telegram 即使在 `<pre>` 區塊內，仍會吃掉行首空白。**

初版把狀態標記放在**行首**（`>` 建議、`*` 遲到、`x` 已過、**空白** 可搭）。結果是：有標記的列正常，而「可搭」列因為整行以空白開頭，縮排被吃掉、整列往左位移：

```
   NO.   DLY    DEP    ARR      ← 正常
>  1218   +0  16:02  16:42      ← 正常
180    +0  16:21  16:52         ← 縮排被吃掉
2194   +0  16:13  16:59         ← 縮排被吃掉
*  122    +0  16:33  17:07      ← 正常
```

修法不是替空白換一個可見字元，而是**消除「行首可能是空白」這個結構**：狀態欄移到行尾並改用英文短字（`REC` / `RISK` / `LATE` / `GONE` / `OK`），第一欄固定為車次且靠左對齊。如此任何一行都不可能以空白開頭；行尾多餘空白被裁掉則無害。

> **不變式**：第一欄必須靠左對齊。由 `TestPreLinesDoNotStartWithSpace` 把關。

#### 四、已評估但未採用的方案

| 方案 | 未採用原因 |
|---|---|
| `expandable_blockquote` | 只能摺疊，不解決寬度 |
| 垂直卡片（每班車兩行） | 寬度無關，但失去橫向一眼比較的可讀性，且垂直長度加倍 |
| `sendPhoto` 傳 PNG | 排版完美，但需內嵌 CJK 字型（執行檔 +8~15MB）與 `golang.org/x/image`，違反 §4.1「盡量只用標準函式庫」；且文字不可複製、不可搜尋、通知預覽只顯示「[照片]」 |

Bot API 10.2（2026-07-14）確認**無原生表格支援**。

---

### 9.1 正常通知

Telegram `sendMessage`，`parse_mode: HTML`。比較表用 `<pre>` 保持等寬，內容全 ASCII（見 §9.0）。

```
通勤簡報 · 8/18 (二) 07:50

建議搭乘 2008 區間快
桃園 08:26 → 臺北 09:02
準點 · 預計 09:22 打卡（餘裕 8 分）

其他選項
NO.   DLY    DEP    ARR
2008   +0  08:26  09:02  REC
1138   +0  08:34  09:14  LATE
1136   +0  08:16  08:57  GONE

DLY 誤點　DEP 發車　ARR 抵北
REC 建議　LATE 遲到　GONE 已過

以 08:20 抵站、末端 20 分計算
資料更新 07:49:53
```

**排版規範**
- 建議班次獨立區塊置頂，一眼可見；區塊標題用 `<b>`
- 表格按演算法排序輸出，`ARR`（預計抵達）為核心欄位
- 狀態欄置於**行尾**（見 §9.0 之三），值為 `REC` 建議、`RISK` 風險、`LATE` 遲到、`GONE` 已過、`OK` 可搭。圖例只列出當次訊息實際出現的值
- 無即時資料的班次，`DLY` 欄顯示 `--` 而非 `+0`，避免把假設誤讀為觀測；表格下方以文字說明
- 常搭班次（`usual_train_nos`）即使被排序擠出名額也必須出現。看到「2008 已過」是使用者要的資訊；表上找不到 2008 則會被誤讀成程式壞了
- 末行固定顯示 `T_ready` 與 `last_mile` 參數，方便察覺參數失準

### 9.2 遲到警示模式（需求 A9 / A10）

當最佳方案 `lateness > 0` 時切換此版型。重點是**把補償方案放在最上面**，因為此時使用者需要的是「我該做什麼」，而不是「哪班車最快」。

```
今日會遲到 · 8/18 (二) 07:50

照常出門搭 1138
誤點 +2 · 桃園 08:36 → 臺北 09:16
預計 09:36 打卡 · 遲到 6 分

提早出門
提早 1 分（08:19 到站）可搭 1136
誤點 +8 · 抵北 09:05 → 09:25 打卡
這樣做可以準時 · 較照常出門少遲到 6 分

誤點證明
抵達後可申請 2008 次（誤點 24 分），足以涵蓋今日遲到
臺北站櫃檯人工申請

全部班次
NO.   DLY    DEP    ARR  MIN
1138   +2  08:36  09:16    6  REC
2008  +24  08:50  09:26   16  LATE
1136   +8  08:24  09:05    0  RISK

DLY 誤點　DEP 發車　ARR 抵北　MIN 遲到分鐘
REC 建議　RISK 風險　LATE 遲到

以 08:20 抵站、末端 20 分計算
```

（實際訊息中「今日會遲到」「提早出門」「誤點證明」「全部班次」及遲到分鐘數為 `<b>` 粗體。）

**版型規則**

- 誤點證明區塊須**指名車次與誤點分鐘數**，你到櫃檯才知道要報什麼；來源依 §7.6 由「已到站且誤點最大」的候選決定，未必是你搭的那班
- 每個方案都必須標出「代價」（早起幾分鐘）與「效果」（遲到幾分）
- 保留「照常出門」基準線於最上方，讓效益差額一目瞭然
- 遲到分鐘欄命名為 `MIN` 而非 `LATE`，以免與狀態欄的 `LATE` 值混淆；不加單位（圖例已說明），MISSED 班次顯示 `-`
- `lateness > severe_delay_threshold` 時，標題改為「今日嚴重延誤」，並附一行「建議直接聯繫主管」。系統**不代擬訊息、不代為通知任何人**
- 全線皆無達門檻的誤點、卻仍會遲到時，改為警示：
  `今日全線無顯著誤點，遲到可能無法取得證明`

### 9.3 降級通知（重要）

任何環節失敗都必須送出，**絕不可靜默結束**：

```
通勤簡報產生失敗

無法取得台鐵即時誤點資料（TDX API 逾時，已重試 3 次），以下為表定時刻

表定時刻（未套用誤點）
NO.     DEP    ARR
1136  08:16  08:57
2008  08:26  09:02
1138  08:34  09:14

請自行以台鐵 App 確認誤點狀況。

以 08:20 抵站、末端 20 分計算
```

（「通勤簡報產生失敗」與「表定時刻（未套用誤點）」為 `<b>` 粗體，表格為 `<pre>`。）

分級策略：

| 失敗環節 | 降級行為 |
|---|---|
| TDX OAuth 失敗 | 送出純警示訊息，無時刻資料 |
| 時刻表取得失敗 | 同上 |
| 即時動態取得失敗 | **仍送出表定時刻表**，明確標示「未套用誤點」 |
| Telegram 送出失敗 | 重試 3 次（指數退避）；仍失敗則寫入 journald `ERROR`，退出碼 1 |

> systemd 可設定 `OnFailure=` 掛一個備援通知單元（例如寄信到自己信箱），處理「連 Telegram 都掛掉」的極端情況。v0.2 再做。

---

## 10. 專案結構與部署

### 10.1 目錄結構

```
tra-commute-bot/
├── cmd/tracommute/
│   └── main.go                 # 進入點、旗標解析、時區內嵌
├── internal/
│   ├── config/
│   │   └── config.go           # YAML + env 載入、驗證
│   ├── tdx/
│   │   ├── auth.go             # OAuth2 client_credentials
│   │   ├── client.go           # HTTP client、循序節流、429 退避
│   │   ├── models.go           # v3 回應 struct
│   │   └── timeparse.go        # HH:mm / HH:mm:ss 雙格式解析（見 §6.6）
│   ├── planner/
│   │   ├── planner.go          # §7 演算法本體
│   │   ├── classify.go         # 可搭乘性 × 準時性 二維分類
│   │   ├── compensate.go       # §7.8 遲到補償方案搜尋
│   │   └── certificate.go      # §7.6 遲到歸屬拆分
│   ├── scheduler/
│   │   ├── scheduler.go        # §10.3 排程規則比對
│   │   └── state.go            # 狀態檔讀寫、重試視窗判斷
│   │   └── planner_test.go     # 表格驅動測試（情境 A–E）
│   ├── notify/
│   │   ├── telegram.go
│   │   └── render.go           # §9 訊息渲染
│   └── clock/
│       └── clock.go            # Clock 介面，供測試注入固定時間
├── configs/
│   └── config.example.yaml
├── deploy/
│   ├── tra-commute.service
│   ├── tra-commute.timer
│   └── install.sh
├── .github/workflows/ci.yml
├── go.mod
└── README.md
```

### 10.2 CLI 旗標

```
tracommute [flags]

  -config string    設定檔路徑（預設 /etc/tra-commute/config.yaml）
  -at string        模擬指定時刻執行，格式 "2006-01-02T15:04"（測試用）
  -dry-run          計算並印出訊息到 stdout，不發送 Telegram
  -verbose          輸出完整 API 回應
```

> 🔑 **`-at` 與 `-dry-run` 是必要功能，不是加分項。** 這支程式一天只在 07:50 跑一次，若沒有辦法在任意時間模擬執行，你將完全無法除錯——遇到問題只能等隔天再看一次。`clock.Clock` 介面必須從第一天就設計進去，事後補會很痛。

### 10.3 排程機制：Tick + Guard

需求 A8 要求「能自行調整通知的星期與時刻」。這排除了把排程寫死在 systemd 或 cron 的做法——那需要 sudo 編輯 unit 檔並 `daemon-reload`，且排程規則會跟 `config.yaml` 分居兩地，形成兩個真實來源。

#### 方案比較

| 方案 | 改排程的成本 | 單一設定來源 | 崩潰韌性 | 支援複雜規則 |
|---|---|---|---|---|
| systemd `OnCalendar` 寫死 | sudo + daemon-reload | ❌ 分兩地 | ✅ | ⚠️ 補班日需另加 unit |
| crontab | `crontab -e` | ❌ 分兩地 | ✅ | ⚠️ 每種規則一行，易亂 |
| 常駐 daemon 自行排程 | 改 config + 重啟 | ✅ | ❌ **當機即永久靜默** | ✅ |
| **Tick + Guard（採用）** | **改 config，存檔即生效** | ✅ | ✅ 下一分鐘自動重試 | ✅ |

#### 採用方案

systemd timer **每分鐘**觸發一次，程式自行判斷該不該動作：

```
每分鐘喚醒
  ├─ 讀 config.yaml
  ├─ 今天在 skip_dates 內？        → 結束
  ├─ 命中任一 schedule 的日期規則？ → 否則結束
  ├─ 現在時刻 ∈ [at, at + tick_tolerance]？ → 否則結束
  ├─ 讀狀態檔：今天這個 schedule 已成功送出？ → 是則結束
  └─ 執行完整流程 → 成功後寫入狀態檔
```

**為什麼這個模式對？**

- **排程規則住在 `config.yaml`** — 改完存檔下一分鐘生效，不用 sudo、不用 daemon-reload、不用重啟
- **process 短命** — 每次執行 < 10 秒即退出，沒有長時間執行的記憶體或 goroutine 洩漏風險
- **崩潰自癒** — 這一分鐘失敗（網路抖動、TDX 暫時性錯誤），下一分鐘自動重試；而常駐 daemon 一旦 panic 就永久靜默，那正是這種系統最可怕的失效模式
- **規則表達力不受限** — 特定星期、特定日期（補班日）、排除日期（請假）、多個通知時段，全部只是 config 的資料
- **v0.3 的「出門前二次查詢」只要多加一筆 schedule**，不動任何程式碼

**成本可忽略**：Go 執行檔冷啟動 < 10 ms、RSS 約 5 MB，一天喚醒 1440 次。絕大多數 tick 在讀完 config 後就結束，**不會呼叫 TDX API**——每日實際 API 用量仍是 4 次左右。

#### 狀態檔

`/var/lib/tra-commute/state.json`（由 systemd `StateDirectory=` 建立）

```json
{
  "last_success": { "平日通勤": "2026-08-18", "補班日": "2026-08-15" },
  "last_attempt": { "平日通勤": "2026-08-18T07:50:03+08:00" },
  "attempt_count_today": { "平日通勤": 1 }
}
```

重試語意：同一天同一 schedule 若尚未成功，允許在 `[at, at + retry_window_minutes]`（預設 10 分）內於每個 tick 重試；超過視窗即放棄並發出降級通知。這避免了「TDX 掛一整天，Telegram 被灌 1440 則失敗訊息」。

#### systemd 單元

`/etc/systemd/system/tra-commute.timer`

```ini
[Unit]
Description=TRA commute brief tick

[Timer]
OnCalendar=*:0/1                # 每分鐘
AccuracySec=1s
RandomizedDelaySec=0
Persistent=false
Unit=tra-commute.service

[Install]
WantedBy=timers.target
```

> 這裡不再需要 `OnCalendar` 的時區後綴，因此**沒有 systemd ≥ 250 的版本要求**——排程語意已完全移進程式內，由 `time.LoadLocation("Asia/Taipei")` 負責。這是 tick + guard 的附帶好處。
> `Persistent=false`：主機當天沒開機時不補跑，因為隔天補送一則過期的通勤簡報毫無意義。

`/etc/systemd/system/tra-commute.service`

```ini
[Unit]
Description=TRA commute brief
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
User=tracommute
Group=tracommute
EnvironmentFile=/etc/tra-commute/env
ExecStart=/usr/local/bin/tracommute -config /etc/tra-commute/config.yaml
StateDirectory=tra-commute

# 安全強化
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ProtectKernelTunables=true
ProtectControlGroups=true
RestrictAddressFamilies=AF_INET AF_INET6
```

> **日誌噪音**：每分鐘一次 oneshot 會在 journald 產生大量 `Started/Finished` 紀錄。未命中排程時程式應以 `slog` 的 `DEBUG` 等級靜默結束，並考慮為此 unit 設定 `LogLevelMax=notice`，避免 `journalctl` 被灌爆。

### 10.4 部署流程

```bash
# 本機交叉編譯
GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -o tracommute ./cmd/tracommute

# 上傳
scp tracommute linode:/tmp/
ssh linode 'sudo install -m 0755 /tmp/tracommute /usr/local/bin/'

# 首次安裝
ssh linode 'sudo bash /path/to/deploy/install.sh'

# 驗證
ssh linode 'sudo -u tracommute tracommute -dry-run -at 2026-08-19T07:50'
ssh linode 'systemctl list-timers tra-commute'
```

### 10.5 可觀測性

- **日誌**：結構化 JSON（`log/slog`）寫 stdout → journald。`journalctl -u tra-commute -n 50`
- **原始資料存檔**：每次執行將三支 API 的原始回應寫入 `/var/lib/tra-commute/dumps/2026-08-18.json`，保留 30 天。
  > 這是事後檢討「為什麼那天推薦錯了」的唯一依據。沒有它，你只能憑記憶猜測。成本極低，價值極高。

---

## 11. 測試策略

### 11.1 單元測試（重點：`internal/planner`）

採**表格驅動測試**，注入固定時鐘與固定的時刻表/誤點資料，涵蓋：

| 測試案例 | 驗證目標 |
|---|---|
| 情境 A：全部準點 | 推薦 2008（1136 已錯過） |
| 情境 B：全部誤點 10 分 | **推薦 1136**（09:07 抵北，早於 2008 的 09:12） |
| 情境 C：2008 重度誤點、1136 輕度 | 推薦 1136，2008 遲到 17 分 |
| 情境 D：邊界，僅差 2 分鐘 | 該班次歸為 RISKY 而非 CATCHABLE |
| 情境 E：全部會遲到 | 推薦遲到最少者，切換警示版型 |
| 補償一：提早出門可救 | 產出「提早 N 分出門」方案且 lateness 確實下降 |
| 證明：可涵蓋 | 選出誤點最大且已到站的車次，判定 covered |
| 證明：未涵蓋 | certifiable < lateness 時給出未必涵蓋的措辭 |
| 證明：全線準點 | 候選集合為空時觸發「可能無法取得證明」警示 |
| 證明：尚未到站者排除 | est_arr 晚於推薦班次的誤點列車不得被選為 best |
| 證明涵蓋警示 | 遲到但誤點未達門檻時觸發「可能開不出證明」 |
| 排程：週間命中 | 週三 07:50 命中「平日通勤」 |
| 排程：補班日命中 | dates 清單內的週六命中 |
| 排程：skip_dates | 命中日期但被排除 |
| 排程：guard 冪等 | 同日第二次 tick 不重複送出 |
| 排程：重試視窗 | 失敗後於視窗內重試，超出視窗停止 |
| 補償：怎樣都救不了 | 不產出假方案，升級為嚴重警示 |
| lateness 邊界：剛好 0 分 | 判定為準時，不觸發警示版型 |
| 誤點為負值 | clamp 至 0 |
| 無即時資料 | `DelaySource=None`，仍納入排序但可信度最低 |
| 車種：排除普悠瑪／太魯閣 | 不出現在候選中 |
| 車種：自強／莒光納入 | 正常參與排序，且不加註記 |
| 車種：未知車種 | 依 include_and_flag 納入並標示警告 |
| 候選清單為空 | 不 panic，送出「查無班次」通知 |
| 跨午夜（防禦性） | 時間比較不出錯；實測時刻表確有 `23:28 → 00:02` 之班次 |
| 時間解析 | `"12:43"` 與 `"14:42:00"` 兩種格式皆正確解析為 Asia/Taipei |
| 429 退避 | 連續 429 時依 5/10/15 秒退避，耗盡後走降級通知 |

**覆蓋率目標**：`internal/planner` ≥ 90%，其餘 ≥ 60%。

### 11.2 整合測試

- TDX client 對 `httptest.Server` 打假回應，驗證 struct 解析、重試、逾時
- Telegram notifier 對 `httptest.Server`，驗證訊息格式與重試

### 11.3 上線驗收

連續 5 個工作日以 `-dry-run` 手動於 07:50 執行，人工比對台鐵官方 App，確認：
1. 誤點數字一致
2. 推薦班次合理
3. 訊息在 iPhone 上排版正常（等寬字未跑版）

---

## 12. 實作順序

建議依此順序，每步都可獨立驗證：

| 階段 | 內容 | 產出驗證 |
|---|---|---|
| 0 | **用 curl / Swagger 打通 TDX，校正 §2.2 時刻表與 §6.4 所有待驗證項目** | 手上有真實 JSON 樣本 |
| 1 | `config` + `clock` + `tdx/auth` | 能拿到 access token |
| 2 | `tdx/client` + `models` | 能印出今日 OD 時刻表 |
| 3 | 接上即時動態，合併資料 | 能印出含誤點的候選清單 |
| 4 | **`planner` + 完整測試** | 情境 A–E 全綠 |
| 5 | `notify/render` + `telegram` | 手機收到排版正確的訊息 |
| 6 | systemd 部署 + 存檔/日誌 | `systemctl list-timers` 正確 |
| 7 | 5 日驗收 + 校正 `ready_lead_minutes` / `last_mile_minutes` | 參數貼近實際 |

> **階段 0 不可跳過。** §6.4 列出的五個待驗證項目，任一個猜錯都會讓後面全部重寫。

---

## 13. 後續版本規劃

| 版本 | 功能 | 說明 |
|---|---|---|
| v0.2 | **多路線／回程支援**（A12） | `route` + `constraints` 抽為 `journeys` 清單，`schedules` 各項可指定 `journey`；未指定則用預設，維持向後相容 |
| v0.2 | 停駛/事故偵測 | 接 `/News`，重大事故時提高警示層級 |
| v0.2 | 政府行事曆整合 | 目前補班日以 `dates` 手動維護，可改為自動判定 |
| v0.3 | 二次查詢 | 08:10 再推一次更新，抓取離發車更近的誤點（此時需實作 token 快取） |
| v0.3 | 高鐵備案 | 全線大誤點時，計算高鐵桃園站方案 |
| v0.4 | 誤點統計 | 累積數月資料，回答「哪班車最常誤點」，據此調整常搭班次 |
| v0.4 | Telegram 互動 | inline keyboard「重新查詢」按鈕 |

---

## 附錄 A：待確認清單

實作前必須解決，否則系統可能給出錯誤建議：

### A.1 需向使用者確認（阻擋規格定案）

✅ **已全數解決。** A1–A13 均已確認，規格可據以實作。

### A.2 需實測驗證（阻擋實作）

已由 `probe-tdx.sh` 於 2026-08-18 解決：

- [x] 1136、1138 真實表定時刻 → 1136 `08:16→08:57`、2008 `08:26→09:02`、1138 `08:34→09:14`
- [x] 情境 B 正解 → **1136**（推翻原始需求敘述，見 §7.7）
- [x] TDX v3 三支端點回應結構 → 見 §6.4
- [x] 日期參數格式 → `yyyy-MM-dd`，無 `/Today/OD` 變體
- [x] `DelayTime` 負值 → 單次取樣未見，clamp 保留

第二輪探測（14:37）追加解決：

- [x] `StationLiveBoard` v3 → **僅 1 筆，不堪使用，v0.1 移除**（§6.6）
- [x] 速率限制行為 → 約 8 次請求後 429，退避重試有效
- [x] `TrainLiveBoard/TrainNo/{no}` 端點存在
- [x] 發現 `Schedule*` 與 `Scheduled*` 命名不一致、`HH:mm` 與 `HH:mm:ss` 並存

仍待解決（**不阻擋開始實作**）：

- [ ] **`/TrainType` 完整車種對照**，以填入 `excluded_train_type_ids`（A13）。普悠瑪與一般自強的 `TrainTypeCode` 同為 `11`，故**必須用 `TrainTypeID` 區分**；修正後的探測腳本會直接印出對照表
- [ ] `TrainStationStatus` 語意（實測恆為 `2`，程式不依賴）

### A.3 需上線後校正（不阻擋，但影響推薦品質）

- [ ] `ready_lead_minutes = 30` 是否符合實際（實測從起床到抵達桃園站需時）
- [ ] `last_mile_options[0].minutes = 20` 是否符合實際（實測台北車站 → 打卡）
- [ ] `risk_margin_minutes = 3` 是否過於保守（觀察誤點數字在出門途中的浮動幅度）
- [ ] `severe_delay_threshold = 30` 是否合理（遲到超過幾分鐘該升級為最高警示）

---

## 附錄 B：變更紀錄

| 日期 | 變更 |
|---|---|
| 2026-08-18 | 初版 |
| 2026-08-18 | 確認 A2–A5；新增 A9 遲到補償需求；**將分類模型由一維列舉重構為「可搭乘性 × 準時性」二維**；新增補償演算法與警示版型 |
| 2026-08-18 | 確認 A8（自訂排程）、A9 縮限為僅「提早出門」、新增 A10 誤點證明；新增 §7.6；**排程改為 Tick + Guard 模式**（改寫 §10.3），移除末端替代路徑與組合搜尋 |
| 2026-08-18 | **A11 定案：排序主鍵回歸「實際抵達時間最早」**；移除 `optimize_for` 設定；探測腳本改為自動選車，站點與車次皆可覆寫 |
| 2026-08-18 | 確認 A12 與 `max_early_leave_minutes=15` |
| 2026-08-18 | **API 探測完成**：時刻表校正（**情境 B 正解為 1136，推翻原始敘述**）、確認回應結構與時間格式、發現 **429 速率限制**（§6.5 改為循序請求＋退避）、發現 `SuspendedFlag` 可免費做停駛偵測；新增 A13 車種過濾需求 |
| 2026-08-18 | **第二輪探測**：`StationLiveBoard` v3 僅回傳 1 筆 → **v0.1 完全移除**，請求數降為 3；發現 `Schedule*` 命名與 `HH:mm:ss` 格式不一致的解析陷阱；確認 429 退避策略有效 |
| 2026-08-18 | **實作完成**（Go 1.22，clean architecture 分層：domain / usecase / adapter）。以 `/TrainType` 實測補完 A13 待辦：**太魯閣 = `1101`、普悠瑪 = `1107`**，確認 `TrainTypeCode` 不可用（自強橫跨 `3` 與 `11`）。另實測發現 OD 端點**只回傳起訖兩站**而非全程，故跨午夜判斷簡化為「抵達早於發車即 +1 日」 |
| 2026-08-18 | **版型依實機測試重訂（新增 §9.0，改寫 §9.1–9.3）**：`<pre>` 內改為 **ASCII only**（中文在 Telegram 等寬字體中並非拉丁字元的剛好兩倍寬，cell 計算法無效）；**移除所有 emoji 與框線表格**，改用 `<b>` 粗體。總寬 40 → 24 字元，解決 iPhone 直式 wrap |
| 2026-08-18 | **狀態標記由行首移至行尾並改用英文短字**（§9.0 之三）：Telegram 即使在 `<pre>` 內也會吃掉行首空白，導致「可搭」列（標記為空白）縮排消失、整列左移。不變式：第一欄必須靠左對齊，任何行都不得以空白開頭 |
| 2026-08-18 | **A13 依使用者更正修訂**：篩選依據改為「是否接受電子票證」而非「是否對號」——自強／莒光可持卡買站票故納入，僅排除普悠瑪／太魯閣；改用排除清單並以 `TrainTypeID`／名稱判別（`TrainTypeCode` 不可靠） |
| 2026-08-18 | **A10 依使用者實測修正**：誤點證明可跨車次開立，「選了慢車開不出證明」的假設作廢；`own_lateness` 概念完全移除；§7.6 改為計算「該申請哪一班車的證明」，並編碼「僅限已到站列車」限制 |